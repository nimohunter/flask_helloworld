apiVersion: v1
kind: Service
metadata:
  name: {{ include "helm-flask-helloworld.fullname" . }}
  labels:
    {{- include "helm-flask-helloworld.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "helm-flask-helloworld.selectorLabels" . | nindent 4 }}
